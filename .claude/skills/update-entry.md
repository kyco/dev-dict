# Update Entry Skill

Update existing terms, types, or tags in the dev-dict project.

1. Find and read the file in `src/data/{terms,types,tags}/{id}.ts`.
2. Apply the requested changes. Never modify the `id` field. Preserve existing translations unless told otherwise.
3. Run `pnpm test`.
